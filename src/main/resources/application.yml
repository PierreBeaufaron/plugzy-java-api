spring:
  application:
    name: plugzy
  config:
    import: "classpath:env.properties"

  datasource:
    url: jdbc:postgresql://localhost:5432/plugzy_db
    username: plugzy
    password: plugzymdp

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    show-sql: true

  flyway:
    enabled: false